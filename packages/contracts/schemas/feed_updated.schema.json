{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "FeedUpdated",
  "description": "Event emitted when a user's feed is updated",
  "type": "object",
  "properties": {
    "user_id": {
      "type": "string",
      "description": "ID of the user whose feed was updated"
    },
    "feed_id": {
      "type": "string",
      "description": "Identifier of the feed"
    },
    "items": {
      "type": "array",
      "description": "List of item identifiers included in the feed",
      "items": {
        "type": "string"
      }
    }
  },
  "required": ["user_id", "feed_id", "items"],
  "additionalProperties": false
}
