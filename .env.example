# Core
NODE_ENV=development
COOKIE_DOMAIN=localhost
CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002
AUTH_PEPPER=dev_pepper_change_me
AGENT_SHARED_TOKEN=dev_shared_token_32_character_secret__
CORE_API_URL=http://localhost:8760

# Agents
NOVA_AGENT_TOKEN=changeme

# Postgres
POSTGRES_USER=nova
POSTGRES_PASSWORD=nova
POSTGRES_DB=nova
DATABASE_URL=postgresql+psycopg2://nova:nova@db:5432/nova

# Redis - Namespace partitioning support
REDIS_URL=redis://redis:6379/0
# REDIS_DB=1  # Override: 0=NovaOS, 1=BlackRose, 2=GypsyCove

# JWT (RS256) — dev keys only (examples)
JWT_ALG=RS256
JWT_PRIVATE_KEY_PATH=./keys/dev_jwt_private.pem
JWT_PUBLIC_KEY_PATH=./keys/dev_jwt_public.pem

# Platform
PLATFORM_CUT=0.12

# App URLs
CORE_API_BASE=http://localhost:8760
ECHO_WS=ws://localhost:8765/ws
SITE_URL=http://localhost:3002
NOVA_CONSOLE_BASE_URL=http://localhost:3001
BRC_DOMAIN=localhost
